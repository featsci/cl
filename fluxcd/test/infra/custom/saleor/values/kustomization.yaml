apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: slr
configMapGenerator:
  - name: slr-values
    files:
      - values.yaml=slr-values.yaml

configurations:
  - kustomizeconfig.yaml