apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "slr.fullname" . }}-test-connection"
  labels:
    {{- include "slr.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "slr.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
