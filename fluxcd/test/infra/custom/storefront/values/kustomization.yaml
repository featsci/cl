apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: dsf
configMapGenerator:
  - name: dsf-values
    files:
      - values.yaml=dsf-values.yaml

configurations:
  - kustomizeconfig.yaml