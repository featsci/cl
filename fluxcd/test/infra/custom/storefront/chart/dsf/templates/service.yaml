apiVersion: v1
kind: Service
metadata:
  name: {{ include "dsf.fullname" . }}
  labels:
    {{- include "dsf.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "dsf.selectorLabels" . | nindent 4 }}
