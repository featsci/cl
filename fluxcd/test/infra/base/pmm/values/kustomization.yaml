apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: pmm
generatorOptions:
  disableNameSuffixHash: true
configMapGenerator:
  - name: pmm-values
    files:
      - values.yaml=pmm-values.yaml
configurations:
  - kustomizeconfig.yaml
