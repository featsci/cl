apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: ot-operators
configMapGenerator:
  - name: ot-redis-operator-values
    files:
      - values.yaml=ot-redis-operator-values.yaml

  - name: ot-redis-replication-values
    files:
      - values.yaml=ot-redis-replication-values.yaml
configurations:
  - kustomizeconfig.yaml
