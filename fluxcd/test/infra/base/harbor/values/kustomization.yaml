apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: harbor
configMapGenerator:
  - name: harbor-values
    files:
      - values.yaml=harbor-values.yaml

configurations:
  - kustomizeconfig.yaml