apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: slr-itops-space-crt-production
  namespace: istio-system
spec:
  secretName: tls-slr-itops-space
  issuerRef:
    kind: ClusterIssuer
    name: letsencrypt-production-key-slr
  commonName: slr.${DOMAIN}
  dnsNames:
  - slr.${DOMAIN}