apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: dsf-itops-space-crt-production
  namespace: istio-system
spec:
  secretName: tls-dsf-itops-space
  issuerRef:
    kind: ClusterIssuer
    name: letsencrypt-production-key-dsf
  commonName: dsf.${DOMAIN}
  dnsNames:
  - dsf.${DOMAIN}