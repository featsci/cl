# ---
# apiVersion: cert-manager.io/v1
# kind: Certificate
# metadata:
#   name: monitoring-itops-space-crt-staging
#   namespace: istio-system # monitoring # istio-system
# spec:
#   secretName: tls-monitoring-itops-space
#   issuerRef:
#     kind: ClusterIssuer
#     name: letsencrypt-${LETS_CERTIFICATE_STAGING}-key-monitoring
#   commonName: monitoring.${DOMAIN}
#   dnsNames:
#   - monitoring.${DOMAIN}
# ---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: monitoring-itops-space-crt-production
  namespace: istio-system # monitoring # istio-system
spec:
  secretName: tls-monitoring-itops-space
  issuerRef:
    kind: ClusterIssuer
    name: letsencrypt-production-key-monitoring
  commonName: monitoring.${DOMAIN}
  dnsNames:
  - monitoring.${DOMAIN}